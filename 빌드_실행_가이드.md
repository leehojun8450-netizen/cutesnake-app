# 📱 폰에 앱으로 설치하기 — 따라하기 가이드

> 목표: 컴퓨터에서 게임을 **APK(앱 설치파일)** 로 만들어 내 폰에 설치하기
> 새로 추가된 **게임 선택 메뉴 + 짝맞추기 게임**이 모두 포함됩니다.

---

## 0️⃣ 준비물 (처음 한 번만)
1. **Android Studio** 설치 — https://developer.android.com/studio (무료)
   - 설치할 때 Android SDK, JDK가 함께 깔립니다.
2. **Node.js** 설치 — https://nodejs.org (LTS 버전)
   - `npx cap sync` 명령을 쓰기 위해 필요합니다.

---

## 1️⃣ 최신 코드 받기
이미 폴더가 있다면 최신으로 업데이트:
```
cd C:\Users\151934\cutesnake-app
git pull origin claude/game-recommendations-40s-f1cf0l
```
폴더가 없다면 GitHub에서 **Code → Download ZIP** 으로 받아 압축을 풉니다.

---

## 2️⃣ ⭐ 게임을 앱에 넣기 (가장 중요!)
프로젝트 폴더에서 아래 두 줄을 차례로 실행하세요.
```
npm install
npx cap sync android
```
- `npm install` : 필요한 도구 설치 (처음 한 번)
- `npx cap sync android` : **www 폴더(게임 코드)를 안드로이드 앱 안으로 복사**
- `✔ Sync finished` 가 나오면 성공!

> ⚠️ 이 단계를 빼먹으면 예전 게임이 그대로 나옵니다. 코드를 수정할 때마다 다시 실행하세요.

---

## 3️⃣ Android Studio에서 열기
```
npx cap open android
```
→ Android Studio가 열리면 하단 진행바가 끝날 때까지 대기 (첫 실행은 수 분, "Gradle sync finished")

---

## 4️⃣ 폰에서 바로 실행해보기 (추천)
1. 폰: **설정 → 개발자 옵션 → USB 디버깅 ON**
   - 개발자 옵션이 없으면: 설정 → 휴대전화 정보 → **빌드번호 7번 연속 탭**
2. USB로 폰 연결 → "USB 디버깅 허용?" → 허용
3. Android Studio 상단에 폰 이름이 뜨면 **▶ Run** 클릭
4. 폰에 앱이 자동 설치·실행 → **🎮 게임 고르기 메뉴**가 나오면 성공!

---

## 5️⃣ 설치파일(APK) 만들어서 카톡으로 받기 (USB 없이)
USB 연결 없이 폰에 깔고 싶다면:
1. Android Studio 메뉴 **Build → Build App Bundle(s) / APK(s) → Build APK(s)**
2. 빌드 끝나면 우측 하단 알림의 **locate** 클릭 →
   `android\app\build\outputs\apk\debug\app-debug.apk` 파일 위치 열림
3. 이 `app-debug.apk` 파일을 **나에게 카톡/메일로 전송** → 폰에서 파일 열어 설치
   - 폰에서 "출처를 알 수 없는 앱 설치 허용" 한 번 켜야 할 수 있어요.

---

## 6️⃣ (선택) 스토어 출시용 빌드 — 나중에
플레이스토어에 올릴 때만 필요합니다.
1. 메뉴 **Build → Generate Signed App Bundle / APK → Android App Bundle**
2. **Create new...** 로 키스토어(.jks) 생성
   - ⚠️ 키스토어 파일 + 비밀번호 **절대 분실 금지** (분실 시 앱 업데이트 영구 불가, 2곳 백업)
3. release 빌드 → `.aab` 파일 생성 → Google Play Console 업로드

---

## 🔄 앞으로 게임을 고치면
게임 코드(`www` 폴더)를 수정한 뒤에는 항상:
```
npx cap sync android
```
그다음 Android Studio에서 다시 **▶ Run** 또는 Build.

---

## ⚠️ 자주 겪는 문제
- **메뉴/게임이 안 바뀜** → `npx cap sync android` 를 안 했거나, Run을 다시 안 함
- **"SDK location not found"** → Android Studio를 한 번 정상 실행하면 자동 생성됨
- **Gradle sync 느림** → 첫 실행은 원래 느림 (인터넷으로 의존성 다운로드)
- **폰이 안 잡힘** → 데이터 전송 되는 USB 케이블인지, USB 디버깅 켰는지 확인
- **JDK 오류** → 설정 → Build Tools → Gradle → Gradle JDK = "jbr-21" 선택

---

## 📂 현재 게임 구성
```
www/
├── index.html    ← 게임 선택 메뉴 (앱 시작 화면)
├── memory.html   ← 🍀 짝 맞추기 (기억력 카드 게임)
└── snake.html    ← 🐍 꼬마 뱀 (사과 모험)
```
각 게임 화면 왼쪽 위 🏠 버튼으로 메뉴로 돌아옵니다.

# 📱 Android Studio 빌드 & 실행 가이드

## ✅ 프로젝트 위치
`C:\Users\151934\cutesnake-app\android`  (Android Studio로 이 폴더를 엶)

## 다시 열려면
```
cd C:\Users\151934\cutesnake-app
npx cap open android
```

---

## 1️⃣ Gradle 동기화 (첫 실행만 수 분)
- 창 하단 진행 바가 끝날 때까지 대기 ("Gradle sync finished")
- "Upgrade AGP/Gradle?" 팝업 → 무시하거나 "Don't remind me"

## 2️⃣ 실행

### 내 폰으로 (추천)
1. 폰: 설정 → 개발자 옵션 → **USB 디버깅 ON**
   - 개발자 옵션 없으면: 설정 → 휴대전화 정보 → **빌드번호 7번 탭**
2. USB 연결 → "USB 디버깅 허용?" 허용
3. 상단에 폰 이름 뜨면 **▶ Run** 클릭 → 폰에 자동 설치·실행

### 에뮬레이터로
1. **Device Manager** → **Create Device** → Pixel 선택 → 이미지 다운로드
2. **▶ Run** → 가상 폰에서 실행

## 3️⃣ 스토어 제출용 빌드 (.aab)
1. 메뉴 **Build → Generate Signed App Bundle / APK**
2. **Android App Bundle** 선택
3. **Create new...** 로 키스토어(.jks) 생성
   - ⚠️ **키스토어 파일 + 비밀번호 절대 분실 금지!** (분실 시 앱 업데이트 영구 불가)
   - 안전한 곳에 백업 (클라우드 + USB 등 2곳)
4. release 빌드 → `.aab` 파일 생성됨
5. 이 파일을 Google Play Console에 업로드

---

## 🔄 게임 수정 후 앱에 반영
게임 파일(`snake_game.html`)을 고쳤다면:
```
copy "C:\Users\151934\Saved Games\snake_game.html" "C:\Users\151934\cutesnake-app\www\index.html"
cd C:\Users\151934\cutesnake-app
npx cap sync
```
그 후 Android Studio에서 다시 Run/빌드.

---

## ⚠️ 자주 겪는 문제
- **"SDK location not found"**: `android/local.properties` 에 `sdk.dir=...` 있는지 확인 (이미 생성됨)
- **Gradle sync 느림**: 첫 실행은 원래 느림 (인터넷으로 의존성 다운로드)
- **폰이 안 잡힘**: USB 케이블이 데이터 전송 지원하는지 확인, USB 디버깅 재확인
- **JDK 오류**: Android Studio 설정 → Build Tools → Gradle → Gradle JDK = "jbr-21" 선택

---

## 📋 출시 전 남은 일
- [ ] 실기기 테스트 (게임 정상 동작 + 진동 확인)
- [ ] 개인정보처리방침 URL 준비
- [ ] 스크린샷 2장+ (폰에서 게임 캡처)
- [ ] Google Play Console 가입 ($25)
- [ ] .aab 업로드 → 심사 제출
